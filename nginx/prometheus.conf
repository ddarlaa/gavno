server {
    listen 8080;
    location /stub_status {
        stub_status;
        allow 127.0.0.1;          # в Docker – разрешаем все или подсеть
        allow 172.0.0.0/8;
        deny all;
    }
}